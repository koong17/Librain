<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="lib.member.book.model.MemBookDAO">
	
	<insert id="hopeInsertBook" parameterType="lib.employee.book.model.Hope_BookDTO">
		insert into hope_book VALUES ((select nvl(max(hope_book_num)+1,1) from hope_book), 
			#{ book_author }, #{ book_name }, #{ book_pub_house }, #{ book_pub_date }, 
			#{ book_ISBN }, #{ book_price }, '신청 완료', sysdate)
	</insert>
	
	<select id="hopeMemCheck" parameterType="java.lang.String" resultType="int">
		select count(*) from member where mem_id = #{ mem_id }
	</select>
	
	<select id="searchAll" parameterType="java.lang.String" resultType="lib.member.book.model.MemBookDTO">
		select b.book_num, book_author, book_pub_house, book_name,  decode(rent_enddate,'o','대여중','대여가능') rent
			from  (select * from book where book_name like #{ searchWord } or book_author like #{ searchWord } or book_pub_house like #{ searchWord }) b 
			left outer join (select book_num, nvl(to_char(rent_enddate),'o') rent_enddate from rental where rent_startdate in (select max(rent_startdate) from rental group by book_num))  r  
			on b.book_num = r.book_num
	</select>
	
	<select id="searchBookName" parameterType="java.lang.String" resultType="lib.member.book.model.MemBookDTO">
		select b.book_num, book_author, book_pub_house, book_name,  decode(rent_enddate,'o','대여중','대여가능') rent
			from  (select * from book where book_name like #{ searchWord }) b 
			left outer join (select book_num, nvl(to_char(rent_enddate),'o') rent_enddate from rental where rent_startdate in (select max(rent_startdate) from rental group by book_num))  r  
			on b.book_num = r.book_num
	</select>
	
	<select id="searchAuthor" parameterType="java.lang.String" resultType="lib.member.book.model.MemBookDTO">
		select b.book_num, book_author, book_pub_house, book_name,  decode(rent_enddate,'o','대여중','대여가능') rent
			from  (select * from book where book_author like #{ searchWord }) b 
			left outer join (select book_num, nvl(to_char(rent_enddate),'o') rent_enddate from rental where rent_startdate in (select max(rent_startdate) from rental group by book_num))  r  
			on b.book_num = r.book_num
	</select>
	
	<select id="searchPub" parameterType="java.lang.String" resultType="lib.member.book.model.MemBookDTO">
		select b.book_num, book_author, book_pub_house, book_name,  decode(rent_enddate,'o','대여중','대여가능') rent
			from  (select * from book where book_pub_house like #{ searchWord }) b 
			left outer join (select book_num, nvl(to_char(rent_enddate),'o') rent_enddate from rental where rent_startdate in (select max(rent_startdate) from rental group by book_num))  r  
			on b.book_num = r.book_num
	</select>
	
	<select id="newBook" parameterType="int" resultType="lib.member.book.model.MemBookDTO">
	<![CDATA[
		select * from
		    (select rownum rnum, b.book_num, book_author, book_pub_house, book_name, book_pub_date, book_ISBN, book_apdx_status, book_rsrv_status, book_ctgr_num, book_input_date, book_rent_cnt,
		            decode(rent_enddate,'o','대여중','대여가능') rent
		            from   (select * from book order by book_input_date desc) b
		            left outer join (select book_num, nvl(to_char(rent_enddate),'o') rent_enddate from rental where rent_startdate in (select max(rent_startdate) from rental group by book_num))  r  
		            on (b.book_num = r.book_num) where rownum<=20)
             where book_input_date<= sysdate and book_input_date >=sysdate-30 
            		and rnum > #{ startRowNum } and rnum <= #{ endRowNum }
            		order by book_num desc
	]]>
	</select>
	
	<select id="mostRent" parameterType="int" resultType="lib.member.book.model.MemBookDTO">
	<![CDATA[
		  select * from
		    (select rownum rnum, b.book_num, book_author, book_pub_house, book_name, book_pub_date, book_ISBN, book_apdx_status, book_rsrv_status, book_ctgr_num, book_input_date, book_rent_cnt,
		            decode(rent_enddate,'o','대여중','대여가능') rent
		            from   (select * from book order by book_rent_cnt desc) b
		            left outer join (select book_num, nvl(to_char(rent_enddate),'o') rent_enddate from rental where rent_startdate in (select max(rent_startdate) from rental group by book_num))  r  
		            on (b.book_num = r.book_num)  where rownum <= 20 )
             where book_input_date<= sysdate and book_input_date >=sysdate-30 
             		and rnum > #{ startRowNum } and rnum <= #{ endRowNum }
             order by book_rent_cnt desc
	]]>
	</select>
	
	
</mapper>