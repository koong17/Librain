<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="lib.member.book.model.BookDAO">

	<select id="searchAll" parameterType="java.lang.String" resultType="lib.member.book.model.BookDTO">
		select b.book_num, book_author, book_pub_house, book_name, nvl2(rent_enddate, '대출가능', '대출중') rent, rent_startdate 
			from (select * from book where book_name like #{ searchWord } or book_author like #{ searchWord } or book_pub_house like #{ searchWord }) b 
			join (select * from rental where rent_startdate in (select max(rent_startdate) from rental group by book_num))  r  
			on b.book_num = r.book_num
	</select>
	
	<select id="searchBookName" parameterType="java.lang.String" resultType="lib.member.book.model.BookDTO">
		select b.book_num, book_author, book_pub_house, book_name, nvl2(rent_enddate, '대출가능', '대출중') rent, rent_startdate 
			from (select * from book where book_name like #{ searchWord }) b 
			join (select * from rental where rent_startdate in (select max(rent_startdate) from rental group by book_num))  r  
			on b.book_num = r.book_num
	</select>
	
	<select id="searchAuthor" parameterType="java.lang.String" resultType="lib.member.book.model.BookDTO">
		select b.book_num, book_author, book_pub_house, book_name, nvl2(rent_enddate, '대출가능', '대출중') rent, rent_startdate 
			from (select * from book where book_author like #{ searchWord }) b 
			join (select * from rental where rent_startdate in (select max(rent_startdate) from rental group by book_num))  r  
			on b.book_num = r.book_num
	</select>
	
	<select id="searchPub" parameterType="java.lang.String" resultType="lib.member.book.model.BookDTO">
		select b.book_num, book_author, book_pub_house, book_name, nvl2(rent_enddate, '대출가능', '대출중') rent, rent_startdate 
			from (select * from book where book_pub_house like #{ searchWord }) b 
			join (select * from rental where rent_startdate in (select max(rent_startdate) from rental group by book_num))  r  
			on b.book_num = r.book_num
	</select>
	
</mapper>